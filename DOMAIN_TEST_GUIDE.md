# 特定领域配置测试指南

本文档介绍 4 种测试特定领域配置的方法。

---

## 🧪 测试方法 1：单元测试（推荐 - 最快）

### 运行所有领域配置测试

```bash
cd /Users/limengya/Work/IdeaProjects/agent0228

# 运行整个测试类
mvn test -Dtest=DomainConfigTest

# 或者运行特定的测试方法
mvn test -Dtest=DomainConfigTest#testAutoDetectMedicalDomain
mvn test -Dtest=DomainConfigTest#testAutoDetectProgrammingDomain
```

### 预期结果

```
----------- DomainConfigTest -----------
✅ testAllDomainPromptsLoaded           - 所有领域提示都已正确加载
✅ testDomainPromptContent              - 每个提示都包含正确的内容
✅ testAutoDetectMedicalDomain          - 医疗问题自动识别
✅ testAutoDetectProgrammingDomain      - 编程问题自动识别
✅ testAutoDetectLegalDomain            - 法律问题自动识别
✅ testAutoDetectFinanceDomain          - 财务问题自动识别
✅ testAutoDetectScienceDomain          - 科学问题自动识别
✅ testSystemPromptBuilderIntegration   - SystemPromptBuilder 集成正常
✅ testAutoDetectionWithSystemPromptBuilder - 完整流程测试通过
✅ testDefaultGeneralPrompt             - 默认提示工作正常
✅ testDetectionPerformance             - 性能测试通过（4000次<5s）

Tests run: 11, Failures: 0, Errors: 0
```

### 测试覆盖的内容

- ✅ 所有 7 个领域提示是否都能正确加载
- ✅ 每个领域提示是否包含关键内容
- ✅ 医疗、编程、法律、财务、科学领域的自动检测
- ✅ 内容创作、数据分析领域的提示是否正常
- ✅ SystemPromptBuilder 是否能正确集成领域提示
- ✅ 完整的自动检测 + 提示构建流程
- ✅ 性能基准测试

---

## 🌐 测试方法 2：REST API 测试（推荐 - 最真实）

### Step 1：启动应用

```bash
cd /Users/limengya/Work/IdeaProjects/agent0228

# 编译
mvn clean package -DskipTests

# 启动应用
export DEEPSEEK_API_KEY="sk-6c677513426744a6833bd437bbc8733c"
java -jar target/agent0228-1.0.0.jar
```

### Step 2：在另一个终端测试不同领域

#### 测试医疗领域（自动检测）
```bash
curl -X POST http://localhost:8080/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{
    "query": "我最近一直头痛，应该吃什么药？",
    "includeDetails": true
  }' | jq .
```

**查看返回结果中的系统提示**（在日志中）：
- 应该包含 "Medical Information Assistant"
- 应该包含医学专业约束条件
- 应该包含"咨询医疗专业人士"的警告

#### 测试编程领域（自动检测）
```bash
curl -X POST http://localhost:8080/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{
    "query": "如何在 Java 中实现单例模式？",
    "includeDetails": true
  }' | jq .
```

**查看返回结果**：
- 应该返回编程相关的答案
- 应该包含代码示例
- 应该包含最佳实践建议

#### 测试法律领域（自动检测）
```bash
curl -X POST http://localhost:8080/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{
    "query": "租赁合同需要注意什么法律问题？",
    "includeDetails": true
  }' | jq .
```

#### 测试财务领域（自动检测）
```bash
curl -X POST http://localhost:8080/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{
    "query": "基金投资有哪些风险需要了解？",
    "includeDetails": true
  }' | jq .
```

#### 测试科学领域（自动检测）
```bash
curl -X POST http://localhost:8080/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{
    "query": "物理学中的万有引力原理是什么？",
    "includeDetails": true
  }' | jq .
```

#### 测试通用领域（无关键词 - 应触发默认提示）
```bash
curl -X POST http://localhost:8080/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{
    "query": "xyzAbcDef",
    "includeDetails": true
  }' | jq .
```

### 预期结果对比

不同领域的回答风格应该明显不同：

| 领域 | 查询 | 预期特征 |
|------|------|--------|
| 医疗 | "头痛怎么办？" | 包含"咨询医生"、"症状"、"警告" |
| 编程 | "如何编码？" | 包含"代码示例"、"最佳实践"、"性能" |
| 法律 | "合同问题？" | 包含"法律"、"咨询律师"、"权利" |
| 财务 | "投资建议？" | 包含"风险免责"、"建议顾问"、"术语" |
| 科学 | "原理解释？" | 包含"科学解释"、"公式"、"论证" |

---

## 🔧 测试方法 3：Web 前端测试（最直观）

### Step 1：启动应用

同上面的 REST API 测试

### Step 2：打开浏览器

```
http://localhost:8080/
```

### Step 3：在前端界面测试各个领域

在对话框中依次输入：

**医疗领域测试查询：**
```
我最近一直头痛，应该吃什么药？
```
观察：
- 回答是否包含健康警告
- 是否建议咨询医疗专业人士

**编程领域测试查询：**
```
如何在 Java 中实现单例模式？
```
观察：
- 是否返回代码示例
- 是否包含性能考虑

**法律领域测试查询：**
```
租赁合同需要注意什么
```
观察：
- 是否提醒咨询律师
- 是否包含法律术语

**财务领域测试查询：**
```
应该如何投资基金
```
观察：
- 是否包含风险免责
- 是否建议咨询财务顾问

**科学领域测试查询：**
```
物理学的万有引力原理
```
观察：
- 是否包含科学解释
- 是否有公式或图表参考

### 前端观察要点

✅ 不同领域的回答风格明显不同
✅ 看到推理过程（Thought → Action → Observation）
✅ 工具调用正常（如果使用了工具）
✅ 执行时间合理（通常 2-5 秒）

---

## 🧬 测试方法 4：源代码调试测试（深度）

### 为 IntelliJ IDEA 设置调试

#### Step 1：配置运行/调试配置

1. 点击 **Run → Edit Configurations**
2. 点击 **+** 创建新配置
3. 选择 **Spring Boot**
4. 填写配置：
   - **Name**: `Debug with Domain Config`
   - **Main class**: `com.agent.Agent0228Application`
   - **VM options**: `-DDEEPSEEK_API_KEY=sk-6c677513426744a6833bd437bbc8733c`
   或使用环境变量：
   - **Environment variables**: `DEEPSEEK_API_KEY=sk-...`

#### Step 2：设置断点

打开以下文件并设置断点：

**DomainPromptConfig.java**
```java
public String detectAndGetDomainPrompt(String query) {
    if (query == null) {  // ← 在这里设置断点 1
        return getGeneralPrompt();
    }
    
    String lowerQuery = query.toLowerCase();
    
    if (containsKeyword(lowerQuery, "医疗", ...)) {  // ← 断点 2
        return getMedicalPrompt();
    }
    // ... 更多领域检测
}
```

**SystemPromptBuilder.java**
```java
public String buildSystemPromptWithDomainDetection(String query) {
    String domainPrompt = domainPromptConfig.detectAndGetDomainPrompt(query);  // ← 断点 3
    return buildSystemPrompt(domainPrompt, query);
}
```

#### Step 3：运行调试

1. 点击 **Run → Debug ...**（或按 Shift+F9）
2. 应用启动后，在浏览器访问 http://localhost:8080/
3. 在前端输入查询，例如："我最近头痛怎么办？"
4. 调试器会在断点处暂停

#### Step 4：检查变量

当程序在断点暂停时，查看：

```
Variables Panel:
├── query = "我最近头痛怎么办？"
├── lowerQuery = "我最近头痛怎么办？"
├── 匹配的关键词 = "医疗" 或 "病" 或 "症状"
└── detectedPrompt = "=== MEDICAL DOMAIN INSTRUCTIONS ===\n..."

Expected:
- query 非空
- lowerQuery 已转小写
- 关键词匹配成功
- detectedPrompt 包含 "Medical"
```

#### Step 5：继续执行

按 F10（Step Over）或 F9（Continue）逐步观察：
- 关键词是否正确匹配
- 是否返回了正确的领域提示
- SystemPromptBuilder 是否正确集成了领域提示

### 调试检查清单

- [ ] 输入的查询是否被正确接收
- [ ] 关键词匹配是否工作
- [ ] 返回的领域提示是否正确
- [ ] 完整提示中是否包含了领域提示内容
- [ ] 完整提示中是否包含了工具列表
- [ ] 完整提示中是否包含了响应格式说明

---

## 📊 快速测试清单

### 最简单的测试（5 分钟）

```bash
# 1. 编译
mvn clean package -DskipTests

# 2. 启动应用
export DEEPSEEK_API_KEY="sk-6c677513426744a6833bd437bbc8733c"
java -jar target/agent0228-1.0.0.jar

# 3. 在另一个终端运行单单元测试
mvn test -Dtest=DomainConfigTest

# 4. 或在浏览器访问
# http://localhost:8080/
# 尝试不同领域的查询
```

### 完整测试（30 分钟）

1. ✅ 运行单元测试（所有 11 个测试通过）
2. ✅ 用 REST API 测试 5 个不同领域
3. ✅ 在前端界面手动测试各领域
4. ✅ 检查日志中的领域检测结果
5. ✅ 用调试器验证关键词匹配逻辑

---

## 🐛 故障排除

### 问题 1：单元测试失败

```
FAILED: testAutoDetectMedicalDomain
Error: AssertionError: 查询 '医疗问题' 应该被识别为医疗领域
```

**原因**：关键词可能不在 `detectAndGetDomainPrompt()` 的检测列表中

**解决**：
```java
// 检查 DomainPromptConfig 中的 detectAndGetDomainPrompt()
if (containsKeyword(lowerQuery, "医疗", "病", "症状", ...)) {  // ← 添加缺失的关键词
    return getMedicalPrompt();
}
```

### 问题 2：Web 前端看不出领域差异

**原因**：完整的系统提示没有被传递到 API

**检查**：
1. 确认 `SystemPromptBuilder` 被正确注入到 `ReasoningEngine`
2. 在 `ReasoningEngine.reason()` 中使用 `buildSystemPromptWithDomainDetection(query)`
3. 查看应用日志中是否有"Registered domain prompt"的消息

### 问题 3：某些领域检测不起作用

**原因**：关键词定义可能不够全面

**解决**：编辑 `DomainPromptConfig.detectAndGetDomainPrompt()` 添加更多关键词：

```java
if (containsKeyword(lowerQuery, 
    "医疗", "病", "症状", "医学",      // 中文关键词
    "medical", "disease", "medicine", // 英文关键词
    "medicine", "health", "diagnos")) {
    return getMedicalPrompt();
}
```

---

## 📝 测试报告模板

当测试完成后，你可以记录：

```markdown
# 域配置测试报告

**测试日期**: 2026-02-28
**测试环境**: Java 8, Spring Boot 2.7.18
**测试人员**: [你的名字]

## 单元测试结果
- 总数: 11 个测试
- 通过: __ 个
- 失败: __ 个
- 错误: __ 个
- 耗时: __ms

## API 测试结果
- 医疗领域: ✅/❌
- 编程领域: ✅/❌
- 法律领域: ✅/❌
- 财务领域: ✅/❌
- 科学领域: ✅/❌

## 前端测试结果
- 界面加载: ✅/❌
- 消息发送: ✅/❌
- 领域识别: ✅/❌
- 响应显示: ✅/❌

## 问题记录
- 问题 1: [描述]
- 问题 2: [描述]

## 结论
[总体测试结果摘要]
```

---

## 🎯 建议的测试顺序

1. **第一步**：运行单元测试（确保基础逻辑正确）
   ```bash
   mvn test -Dtest=DomainConfigTest
   ```

2. **第二步**：启动应用并用 REST API 测试各领域
   ```bash
   curl -X POST http://localhost:8080/api/agent/chat ...
   ```

3. **第三步**：用前端界面进行手动测试
   ```
   http://localhost:8080/
   ```

4. **第四步**：如有问题，使用调试器深入分析
   ```
   IDE → Run → Debug → 设置断点
   ```

这样从快速到深入，逐步验证每个层面的功能！
